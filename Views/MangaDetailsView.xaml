<UserControl x:Class="MangaMate.Views.MangaDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MangaMate.Views"
             xmlns:conv="clr-namespace:MangaMate.Converters"
             xmlns:viewModel="clr-namespace:MangaMate.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewModel:MangaDetailsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Command="{Binding BackCommand}" 
                Content="Назад" HorizontalAlignment="Left" Margin="10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <StackPanel Orientation="Horizontal">
                    <Image Source="{Binding Manga.Avatar, Converter={StaticResource ByteArrayToImageConverter}}"
                           Width="200" Height="300" Margin="0,0,20,0"/>

                    <StackPanel>
                        <TextBlock Text="{Binding Manga.Title}" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Manga.Author}" FontSize="16" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Manga.Release}" FontSize="14" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Manga.BookState.Name}" FontSize="14" Margin="0,5,0,0"/>

                        <ItemsControl ItemsSource="{Binding Manga.Genres}" Margin="0,10,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#A03030" CornerRadius="10" Margin="0,0,5,5" Padding="5">
                                        <TextBlock Text="{Binding Name}" Foreground="White"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="{Binding Manga.Description}" Margin="0,20,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
